import SwiftUI

@main
struct QtunnelApp: App {
    var body: some Scene {
        WindowGroup {
            // UI root 将在 P7 替换
            Text("Qtunnel")
                .font(DS.Font.title)
                .foregroundStyle(DS.Color.labelPrimary)
        }
    }
}
